# Environment Configuration Example
# Currently, this application does not require any environment variables.
# This file is provided as a placeholder for enterprise configuration-driven 
# development standards (e.g. database URLs for persisting long-chain data).

MAX_WORKER_THREADS=12
DEFAULT_SEARCH_LIMIT=1000000
DEBUG_LOGGING=false
